{
  "file_name": "TASKS.md",
  "content": "# Plan de Transformation BMAD - Portfolio V2.0\n\nCe document liste les tâches techniques pour migrer du portfolio Legacy vers l'architecture React/Vite/TS.\n\n---\n\n## Phase 1 : Build & Socle Technique (Foundation)\n**Objectif :** Initialiser l'environnement de développement moderne et structurer le projet.\n\n- [ ] **T1.1** : Initialiser le projet Vite + React + TypeScript.\n    - *Cible :* `/package.json`, `/vite.config.ts`\n    - *Legacy :* N/A (Remplacement de l'import manuel de scripts dans `index.html`)\n\n- [ ] **T1.2** : Configurer Tailwind CSS et le support SCSS (Sass).\n    - *Cible :* `/tailwind.config.js`, `/src/index.css`\n    - *Legacy :* `css/index.css` (Récupérer les couleurs du gradient linéaire pour la config Tailwind)\n\n- [ ] **T1.3** : Mettre en place le Routing (React Router v6).\n    - *Cible :* `/src/App.tsx`, `/src/routes.tsx`\n    - *Legacy :* Remplace la navigation via `<a>` et iframes dans `index.html`\n\n- [ ] **T1.4** : Créer l'arborescence des dossiers selon ARCHITECTURE.md.\n    - *Cible :* `/src/core`, `/src/features`, `/src/shared`, `/src/assets`\n    - *Legacy :* Structure à plat actuelle (`css/`, `js/`, `html/`)\n\n- [ ] **T1.5** : Migrer les Assets statiques (Fonts, Images).\n    - *Cible :* `/src/assets/fonts`, `/src/assets/textures`\n    - *Legacy :* Dossiers `fonts/` et `textures/`\n\n---\n\n## Phase 2 : Data-Driven Architecture (Modeling)\n**Objectif :** Séparer le contenu de la vue en créant les services et les fichiers de données.\n\n- [ ] **T2.1** : Extraire le contenu textuel vers des fichiers JSON.\n    - *Cible :* `/public/data/profile.json`, `/public/data/timeline.json` (AboutMe), `/public/data/projects.json`\n    - *Legacy :* Textes hardcodés dans `html/aboutMe.html`, `js/aboutMe.js` (variable `steps`), `html/projects.html`\n\n- [ ] **T2.2** : Définir les Interfaces TypeScript (Strong Typing).\n    - *Cible :* `/src/core/types/index.ts`\n    - *Legacy :* Déduire les types des objets dans `js/aboutMe.js`\n\n- [ ] **T2.3** : Implémenter le `DataService` (Singleton).\n    - *Cible :* `/src/core/services/DataService.ts`\n    - *Legacy :* N/A (Nouvelle couche d'abstraction pour éviter le hardcoding)\n\n- [ ] **T2.4** : Créer un composant de test \"DebugData\" pour valider le fetch.\n    - *Cible :* `/src/features/debug/DebugData.tsx`\n    - *Legacy :* N/A (Validation technique)\n\n---\n\n## Phase 3 : Composants UI & Design System (Modularize)\n**Objectif :** Créer les briques visuelles réutilisables (Atomic Design).\n\n- [ ] **T3.1** : Intégrer les polices et variables CSS globales.\n    - *Cible :* `/src/assets/styles/fonts.scss`, `/src/assets/styles/variables.scss`\n    - *Legacy :* `@font-face` duplications dans tous les fichiers `.css`\n\n- [ ] **T3.2** : Créer le composant `NeonTitle`.\n    - *Cible :* `/src/shared/components/atoms/NeonTitle.tsx`\n    - *Legacy :* `css/neon.css` (Animation `@keyframes blink`)\n\n- [ ] **T3.3** : Créer le composant `ProjectCard` (avec effet 3D).\n    - *Cible :* `/src/shared/components/molecules/ProjectCard.tsx`\n    - *Legacy :* `css/projects.css` (`.row-link a img`, animation `rotateCard`)\n\n- [ ] **T3.4** : Créer le composant `OrbitButton` (Navigation Home).\n    - *Cible :* `/src/shared/components/molecules/OrbitButton.tsx`\n    - *Legacy :* `css/index.css` (`.orbiter`), logique de positionnement extraite de `js/orbit.js`\n\n- [ ] **T3.5** : Créer le `MainLayout` (Header, Bouton Retour, Background).\n    - *Cible :* `/src/layouts/MainLayout.tsx`\n    - *Legacy :* Structure répétée dans chaque fichier HTML (`.container`, `.back-arrow`)\n\n---\n\n## Phase 4 : Implémentation des Features (Pages)\n**Objectif :** Reconstruire les pages en assemblant composants et données.\n\n- [ ] **T4.1** : Feature Home - `FlyText` Component.\n    - *Cible :* `/src/features/home/FlyText.tsx`\n    - *Legacy :* `js/flytext.js` (Logique jQuery à convertir en React `useEffect`/`setTimeout`)\n\n- [ ] **T4.2** : Feature Home - `OrbitMenu` Manager.\n    - *Cible :* `/src/features/home/OrbitMenu.tsx`\n    - *Legacy :* `js/orbit.js` (Calculs trigonométriques `Math.cos/sin` à adapter en TS)\n\n- [ ] **T4.3** : Feature About - `TimeTravel` (Le Convecteur).\n    - *Cible :* `/src/features/about/TimeTravel.tsx`\n    - *Legacy :* `js/aboutMe.js` (Logique `updateView`, gestion des clics et états `active`)\n\n- [ ] **T4.4** : Feature Search - `MatrixMode` (Red/Blue Pill).\n    - *Cible :* `/src/features/search/MatrixToggle.tsx`\n    - *Legacy :* `js/searching.js` (Fonction `toggleWithGlitch` et gestion des classes)\n\n- [ ] **T4.5** : Feature Skills - `SkillCloud` ou Liste.\n    - *Cible :* `/src/features/skills/SkillList.tsx`\n    - *Legacy :* `html/skills.html` (Contenu statique à dynamiser via `DataService`)\n\n---\n\n## Phase 5 : Advanced Visuals & Deployment (Deploy)\n**Objectif :** Optimiser les performances (Canvas) et industrialiser.\n\n- [ ] **T5.1** : Créer `AnimationService` pour gérer les Canvas.\n    - *Cible :* `/src/core/services/AnimationService.ts`\n    - *Legacy :* Centraliser les boucles `requestAnimationFrame` dispersées dans `grid.js`, `tubes.js`, `loading.js`\n\n- [ ] **T5.2** : Implémenter `RetroGrid` (Canvas).\n    - *Cible :* `/src/features/visuals/RetroGrid.tsx`\n    - *Legacy :* `js/grid.js` (Logique de dessin `ctx.moveTo/lineTo`)\n\n- [ ] **T5.3** : Implémenter les Transitions de Page (Warp Speed).\n    - *Cible :* `/src/shared/components/PageTransition.tsx`\n    - *Legacy :* `js/appelLoading.js` + `js/loading.js` (Remplacer l'iframe par un composant overlay React)\n\n- [ ] **T5.4** : Configuration Docker.\n    - *Cible :* `/Dockerfile`, `/nginx.conf`\n    - *Legacy :* N/A\n\n- [ ] **T5.5** : Pipeline GitHub Actions.\n    - *Cible :* `/.github/workflows/main.yml`\n    - *Legacy :* N/A\n"
}